// storing the famous person name in a variable as a string
let famous_person: string = "Martin Luther Kins Jr.";

// storing the message in a variable as a string
let message: string = '"One day we will learn that the heart can never be totally right when the head is totally wrong".';

console.log(`${famous_person} once said, ${message}`);
