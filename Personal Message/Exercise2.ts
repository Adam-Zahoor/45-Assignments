// storing the person name in a variable
let personName: string = "Adam";

// creating a template literal to store the message 
let m2 = `Hello ${personName}, would you like to learn some python today?`;
console.log(m2);
